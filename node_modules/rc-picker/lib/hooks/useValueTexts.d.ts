import { GenerateConfig } from '../generate';
import { Locale } from '../interface';
export default function useValueTexts<DateType>(value: DateType | null, { formatList, generateConfig, locale, }: {
    formatList: string[];
    generateConfig: GenerateConfig<DateType>;
    locale: Locale;
}): string[];
